NUXT_PUBLIC_MOBILE_APP=true

# Dev local — proxy do Vite em /api → backend em :3001
NUXT_PUBLIC_API_BASE=/api

# Produção (Coolify): mantenha /api — o nginx do cliente faz proxy para apiclube (sem CORS).
# NÃO use URL absoluta da API no build do app paciente.
