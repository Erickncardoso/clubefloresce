{
  "name": "clubeflorescer",
  "private": true,
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:backend:tunnel": "node scripts/dev-with-tunnel.mjs",
    "dev:backend:tunnel:named": "node scripts/dev-with-tunnel.mjs --named",
    "dev:tunnel": "node scripts/cloudflare-tunnel.mjs",
    "dev:tunnel:named": "node scripts/cloudflare-tunnel.mjs --named",
    "dev:cliente:tunnel": "node scripts/dev-cliente-tunnel.mjs",
    "dev:tunnel:cliente": "node scripts/dev-cliente-tunnel.mjs",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:cliente": "npm run dev --prefix cliente",
    "dev:paciente": "npm run dev --prefix cliente",
    "dev:mobile": "npm run dev --prefix cliente",
    "dev:mobile:lan": "npm run dev:lan --prefix cliente",
    "generate:cliente": "npm run generate --prefix cliente",
    "generate:paciente": "npm run generate --prefix cliente",
    "build:paciente": "npm run generate --prefix cliente",
    "ag-kit:status": "npx @vudovn/ag-kit status",
    "ag-kit:update": "npx @vudovn/ag-kit update"
  }
}
